<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proyecto.veplaca">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".mainActivity" android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".secondActivity" android:label="Dias para 1-2" android:parentActivityName=".mainActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".mainActivity"/>
        </activity>

        <activity android:name=".thirdActivity" android:label="Dias para 3-4" android:parentActivityName=".mainActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".mainActivity"/>
        </activity>

        <activity android:name=".fourthActivity" android:label="Dias para 5-6" android:parentActivityName=".mainActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".mainActivity"/>
        </activity>

        <activity android:name=".fifthActivity" android:label="Dias para 7-8" android:parentActivityName=".mainActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".mainActivity"/>
        </activity>

        <activity android:name=".sixthActivity" android:label="Dias para 9-0" android:parentActivityName=".mainActivity">
            <meta-data android:name="android.support.PARENT_ACTIVITY" android:value=".mainActivity"/>
        </activity>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts"/>
    </application>

</manifest>